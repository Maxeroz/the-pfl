import styled from "styled-components";

const HeaderUserBox = styled.header`
  display: flex;
  justify-content: space-between;
  padding: 32px 0;
  height: 100px;

  border-bottom: 1px solid var(--input-border-color);
`;

export default HeaderUserBox;
